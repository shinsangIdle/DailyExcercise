action (GetCountSet) {
  description (--)
  collect {
    input (countCom) {
      type (CountCom)
      min (Required) max (One)
    }
    input (exerciseName) {
      type (ExerciseName)
      min (Optional) max (One)
    }
    
  }
  type(Search)
  output (CountAudio)
}
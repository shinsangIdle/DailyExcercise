action (GetRecommend) {
  description (원하는 동작 자세를 검색하는 액션)
  collect {
    input (recommend) {
      type (Recommend)
      min (Optional) max (Many)
    }
  }
  type(Search)  
  output (RecommendResult)
  
}

action (GetRecommend) {
  description (운동루틴을 추천받는 액션)
  collect {
    input (recommend) {
      type (Recommend)
      min (Optional) max (Many)
    }
  }
  type(Search)  
  output (RecommendResult)
  
}

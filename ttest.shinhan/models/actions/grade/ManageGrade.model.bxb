action (ManageGrade) {
  type(Search)
  description (운동 난이도 조절)
  collect {

    input (need) {
      type (Need)
      min (Required) max (One)
    }

    input (grade) {
      type (Grade)
      min (Required) max (One)
    }
  }
  output (resultManageGrade)
}

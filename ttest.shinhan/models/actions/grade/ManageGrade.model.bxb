action (ManageGrade) {
  type(Search)
  description (__DESCRIPTION__)
  collect {

    input (need) {
      type (Need)
      min (Required) max (One)
    }

    input (grade) {
      type (Grade)
      min (Required) max (One)
    }
  }
  output (resultManageGrade)
}

action (GetCountSet) {
  description (--)
  collect {
    input (countCom) {
      type (CountCom)
      min (Required) max (One)
    }
    input (exeName) {
      type (ExeName)
      min (Optional) max (One)
    }
    
  }
  type(Search)
  output (CountAudio)
}